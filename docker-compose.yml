services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: cipra-backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    environment:
      - PYTHONUNBUFFERED=1
    command: >
      python manage.py runserver 0.0.0.0:8000
